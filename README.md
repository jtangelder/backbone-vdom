# Backbone.Views with VirtualDom rendering

````
npm install
npm install webpack -g
````

````
make server
make webpack
open http://localhost:8000
````

### Todo
- Improve JSX transformer to add widgets by calling `<ItemView model={model} />` instead of the `vDomAppendView()` method.
